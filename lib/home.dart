import 'package:flutter/material.dart';
import 'package:transcaption/editor/editor_sections.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Sections(),
    );
  }
}
